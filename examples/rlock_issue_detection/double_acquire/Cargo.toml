[package]
name = "rlock-double-acquire"
version = "0.1.0"
edition = "2021"

[dependencies]
contracts = { version = "0.6.2", features = ["mirai_assertions"] }
mirai-annotations = { path = "../../../annotations" }
